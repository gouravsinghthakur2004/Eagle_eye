import { ResultItem } from '@/types';

export const resultService = {
  getResults: async (): Promise<ResultItem[]> => {
    // TODO: Integrate real API call to fetch live telemetry & leaderboard results
    return [];
  },
};
