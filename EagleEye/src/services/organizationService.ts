import { OrganizationItem } from '@/types';

export const organizationService = {
  getOrganizations: async (): Promise<OrganizationItem[]> => {
    // TODO: Integrate real API call to fetch organizations list
    return [];
  },
};
